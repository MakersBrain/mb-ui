// svelte-check needs a config to know how to parse the components. There is no
// preprocessor: the package ships plain Svelte 5 source with no TypeScript in
// the markup, so a consumer on any toolchain can compile it.
export default {};
